console.log(myVar);
console.log(i);

// another new keyword we can use along with "let" is "const." as far as scope is concerned, let and const behave the same.
const myVar = "what up";

console.log(myVar);

// what error do we get if i is a const, though?
for (const i = 0; i < 5; i++) {

}